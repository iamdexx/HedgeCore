# Approval flow notes

The trade UI should use a single primary sell button. When allowance is insufficient, that button submits the required approval transaction. Once approved, the same button submits the sell transaction.
